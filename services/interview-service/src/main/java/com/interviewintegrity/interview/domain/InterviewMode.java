package com.interviewintegrity.interview.domain;

/** Delivery mode of an interview. */
public enum InterviewMode {
  /** Conducted over a remote meeting link. */
  ONLINE,
  /** Conducted at a physical location. */
  ONSITE,
  /** Mixed remote and on-site participation. */
  HYBRID
}
